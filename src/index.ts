import * as dotenv from 'dotenv';
dotenv.config();

import {bot} from "./bot";

bot.onText(/\/connect/, msg => {

});

bot.onText(/\/send_tx/, msg => {

});

bot.onText(/\/disconnect/, msg => {

});

bot.onText(/\/my_wallet/, msg => {

});
